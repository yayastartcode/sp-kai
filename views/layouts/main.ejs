<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <% if (typeof settings !=='undefined' && settings.logo_image) { %>
        <link rel="icon" type="image/png" href="<%= settings.logo_image %>">
        <% } %>
            <style>
                .gradient-bg {
                    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                }
            </style>
</head>

<body class="bg-gray-50 min-h-screen flex flex-col">
    <%- include('../partials/header') %>

        <main class="flex-grow">
            <% if (typeof success !=='undefined' && success) { %>
                <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative max-w-6xl mx-auto mt-4"
                    role="alert">
                    <span class="block sm:inline">
                        <%= success %>
                    </span>
                </div>
                <% } %>

                    <% if (typeof error !=='undefined' && error) { %>
                        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative max-w-6xl mx-auto mt-4"
                            role="alert">
                            <span class="block sm:inline">
                                <%= error %>
                            </span>
                        </div>
                        <% } %>

                            <%- body %>
        </main>

        <%- include('../partials/footer') %>
</body>

</html>
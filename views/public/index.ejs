<!-- Hero Section -->
<section class="bg-gray-50 py-12 md:py-20 min-h-[500px] flex items-center">
    <div class="max-w-6xl mx-auto px-4 w-full">
        <div class="grid md:grid-cols-2 gap-8 md:gap-12 items-center">
            <!-- Left: Hero Text -->
            <div class="order-2 md:order-1 text-center md:text-left">
                <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold mb-6 leading-tight text-gray-800">
                    <%= settings.hero_title || 'Selamat Datang di Serikat' %>
                </h1>
                <p class="text-lg md:text-xl mb-8 text-gray-600 leading-relaxed">
                    <%= settings.hero_subtitle || 'Platform keanggotaan modern untuk komunitas yang solid' %>
                </p>
                <div class="flex flex-col sm:flex-row gap-4 justify-center md:justify-start">
                    <a href="/register"
                        class="bg-orange-600 text-white px-8 py-3 rounded-lg font-semibold hover:bg-orange-700 transition text-center">
                        Daftar Sekarang
                    </a>
                    <a href="/tentang"
                        class="border-2 border-orange-600 text-orange-600 px-8 py-3 rounded-lg font-semibold hover:bg-orange-600 hover:text-white transition text-center">
                        Pelajari Lebih
                    </a>
                </div>
            </div>

            <!-- Right: Image Slider -->
            <div class="order-1 md:order-2">
                <div class="relative rounded-2xl overflow-hidden shadow-2xl">
                    <div id="hero-slider" class="relative h-64 md:h-80 lg:h-96">
                        <% if (heroSlides && heroSlides.length> 0) { %>
                            <% heroSlides.forEach((item, index)=> { %>
                                <div
                                    class="hero-slide absolute inset-0 transition-opacity duration-500 <%= index === 0 ? 'opacity-100' : 'opacity-0' %> bg-gradient-to-br from-gray-100 to-gray-200 flex items-center justify-center">
                                    <img src="<%= item.image %>" alt="<%= item.title || 'Hero Image' %>"
                                        class="max-w-full max-h-full object-contain">
                                    <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"></div>
                                </div>
                                <% }) %>
                                    <% } else { %>
                                        <div
                                            class="hero-slide absolute inset-0 bg-gray-200 flex items-center justify-center">
                                            <div class="text-center text-gray-400">
                                                <i class="fas fa-images text-6xl mb-4"></i>
                                                <p>Tambahkan gambar di Hero Slider</p>
                                            </div>
                                        </div>
                                        <% } %>
                    </div>

                    <% if (heroSlides && heroSlides.length> 1) { %>
                        <!-- Slider Controls -->
                        <button id="prev-slide"
                            class="absolute left-3 top-1/2 -translate-y-1/2 bg-black/30 hover:bg-black/50 text-white w-10 h-10 rounded-full flex items-center justify-center transition backdrop-blur-sm">
                            <i class="fas fa-chevron-left"></i>
                        </button>
                        <button id="next-slide"
                            class="absolute right-3 top-1/2 -translate-y-1/2 bg-black/30 hover:bg-black/50 text-white w-10 h-10 rounded-full flex items-center justify-center transition backdrop-blur-sm">
                            <i class="fas fa-chevron-right"></i>
                        </button>

                        <!-- Slider Dots -->
                        <div class="absolute bottom-4 left-1/2 -translate-x-1/2 flex gap-2">
                            <% heroSlides.forEach((item, index)=> { %>
                                <button
                                    class="slider-dot w-2 h-2 rounded-full transition <%= index === 0 ? 'bg-white' : 'bg-white/50' %>"
                                    data-index="<%= index %>"></button>
                                <% }) %>
                        </div>
                        <% } %>
                </div>
            </div>
        </div>
    </div>
</section>

<% if (heroSlides && heroSlides.length> 1) { %>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const slides = document.querySelectorAll('.hero-slide');
            const dots = document.querySelectorAll('.slider-dot');
            const prevBtn = document.getElementById('prev-slide');
            const nextBtn = document.getElementById('next-slide');
            let currentSlide = 0;
            let autoSlideInterval;

            function showSlide(index) {
                slides.forEach((slide, i) => {
                    slide.classList.toggle('opacity-100', i === index);
                    slide.classList.toggle('opacity-0', i !== index);
                });
                dots.forEach((dot, i) => {
                    dot.classList.toggle('bg-white', i === index);
                    dot.classList.toggle('bg-white/50', i !== index);
                });
                currentSlide = index;
            }

            function nextSlide() {
                showSlide((currentSlide + 1) % slides.length);
            }

            function prevSlide() {
                showSlide((currentSlide - 1 + slides.length) % slides.length);
            }

            function startAutoSlide() {
                autoSlideInterval = setInterval(nextSlide, 4000);
            }

            function resetAutoSlide() {
                clearInterval(autoSlideInterval);
                startAutoSlide();
            }

            if (prevBtn) prevBtn.addEventListener('click', () => { prevSlide(); resetAutoSlide(); });
            if (nextBtn) nextBtn.addEventListener('click', () => { nextSlide(); resetAutoSlide(); });

            dots.forEach((dot, index) => {
                dot.addEventListener('click', () => { showSlide(index); resetAutoSlide(); });
            });

            startAutoSlide();
        });
    </script>
    <% } %>

        <!-- Features Section -->
        <section class="py-20 bg-gradient-to-b from-gray-50 to-white">
            <div class="max-w-6xl mx-auto px-4">
                <div class="text-center mb-16">
                    <h2 class="text-4xl font-bold text-gray-800 mb-4">Komitmen Kami Untuk Anda</h2>
                    <p class="text-xl text-gray-600">Tiga pilar utama yang menjadi fondasi organisasi kami</p>
                    <div class="w-16 h-1 bg-gradient-to-r from-transparent via-orange-600 to-transparent mx-auto mt-4">
                    </div>
                </div>

                <div class="grid md:grid-cols-3 gap-8">
                    <!-- Feature 1 -->
                    <div
                        class="group bg-white rounded-xl p-10 shadow-md hover:shadow-2xl transition duration-300 border-t-4 border-yellow-400">
                        <div class="text-7xl mb-6 text-yellow-400 group-hover:scale-110 transition duration-300">
                            <i class="fas fa-file-contract"></i>
                        </div>
                        <h3 class="text-2xl font-bold mb-4 text-gray-800">
                            <%= settings.feature_1_title || 'Perjuangan Hak Pekerja' %>
                        </h3>
                        <p class="text-gray-600 leading-relaxed">
                            <%= settings.feature_1_description || 'Kami berjuang untuk melindungi hak-hak pekerja' %>
                        </p>
                        <div class="mt-6 pt-6 border-t border-gray-200">
                            <a href="/tentang"
                                class="text-yellow-600 font-semibold hover:text-yellow-700 flex items-center gap-2">
                                Pelajari lebih lanjut <i class="fas fa-arrow-right text-sm"></i>
                            </a>
                        </div>
                    </div>

                    <!-- Feature 2 -->
                    <div
                        class="group bg-white rounded-xl p-10 shadow-md hover:shadow-2xl transition duration-300 border-t-4 border-blue-500">
                        <div class="text-7xl mb-6 text-blue-500 group-hover:scale-110 transition duration-300">
                            <i class="fas fa-shield-alt"></i>
                        </div>
                        <h3 class="text-2xl font-bold mb-4 text-gray-800">
                            <%= settings.feature_2_title || 'Perlindungan Hukum' %>
                        </h3>
                        <p class="text-gray-600 leading-relaxed">
                            <%= settings.feature_2_description || 'Memberikan dukungan dan konsultasi hukum' %>
                        </p>
                        <div class="mt-6 pt-6 border-t border-gray-200">
                            <a href="/tentang"
                                class="text-blue-600 font-semibold hover:text-blue-700 flex items-center gap-2">
                                Pelajari lebih lanjut <i class="fas fa-arrow-right text-sm"></i>
                            </a>
                        </div>
                    </div>

                    <!-- Feature 3 -->
                    <div
                        class="group bg-white rounded-xl p-10 shadow-md hover:shadow-2xl transition duration-300 border-t-4 border-red-500">
                        <div class="text-7xl mb-6 text-red-500 group-hover:scale-110 transition duration-300">
                            <i class="fas fa-hands-helping"></i>
                        </div>
                        <h3 class="text-2xl font-bold mb-4 text-gray-800">
                            <%= settings.feature_3_title || 'Solidaritas Berserikat' %>
                        </h3>
                        <p class="text-gray-600 leading-relaxed">
                            <%= settings.feature_3_description || 'Membangun komunitas yang solid dan saling mendukung'
                                %>
                        </p>
                        <div class="mt-6 pt-6 border-t border-gray-200">
                            <a href="/tentang"
                                class="text-red-600 font-semibold hover:text-red-700 flex items-center gap-2">
                                Pelajari lebih lanjut <i class="fas fa-arrow-right text-sm"></i>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- About Section -->
        <section id="tentang" class="py-16">
            <div class="max-w-6xl mx-auto px-4">
                <div class="grid md:grid-cols-2 gap-12 items-center">
                    <div>
                        <h2 class="text-3xl font-bold mb-6 text-gray-800">
                            <%= settings.about_title || 'Tentang Kami' %>
                        </h2>
                        <p class="text-gray-600 leading-relaxed">
                            <%= settings.about_content || 'Deskripsi tentang organisasi Anda.' %>
                        </p>
                        <a href="/tentang" class="inline-block mt-6 text-orange-600 font-semibold hover:underline">
                            Selengkapnya <i class="fas fa-arrow-right ml-2"></i>
                        </a>
                    </div>
                    <div class="bg-gray-200 rounded-lg h-80 md:h-96 flex items-center justify-center overflow-hidden">
                        <% if (settings.about_image && settings.about_image !=='/images/about.jpg' ) { %>
                            <img src="<%= settings.about_image %>" alt="About"
                                class="w-full h-full object-cover rounded-lg">
                            <% } else { %>
                                <i class="fas fa-building text-6xl text-gray-400"></i>
                                <% } %>
                    </div>
                </div>
            </div>
        </section>

        <!-- Gallery Section -->
        <section id="galeri" class="py-16 bg-gray-100">
            <div class="max-w-6xl mx-auto px-4">
                <h2 class="text-3xl font-bold text-center mb-12 text-gray-800">Galeri</h2>

                <% if (gallery && gallery.length> 0) { %>
                    <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                        <% gallery.forEach(item=> { %>
                            <a href="/galeri/<%= item.slug || item.id %>"
                                class="relative group overflow-hidden rounded-lg block">
                                <img src="<%= item.image %>" alt="<%= item.title %>"
                                    class="w-full h-48 object-cover group-hover:scale-110 transition duration-300">
                                <div
                                    class="absolute inset-0 bg-black bg-opacity-50 opacity-0 group-hover:opacity-100 transition flex items-center justify-center">
                                    <span class="text-white text-center px-2">
                                        <i class="fas fa-eye text-xl mb-2 block"></i>
                                        <%= item.title %>
                                    </span>
                                </div>
                            </a>
                            <% }) %>
                    </div>
                    <div class="text-center mt-8">
                        <a href="/galeri" class="text-orange-600 font-semibold hover:underline">
                            Lihat Semua Galeri <i class="fas fa-arrow-right ml-2"></i>
                        </a>
                    </div>
                    <% } else { %>
                        <p class="text-center text-gray-500">Belum ada foto di galeri.</p>
                        <% } %>
            </div>
        </section>

        <!-- News Section -->
        <section id="berita" class="py-16">
            <div class="max-w-6xl mx-auto px-4">
                <h2 class="text-3xl font-bold text-center mb-12 text-gray-800">Berita Terbaru</h2>

                <% if (news && news.length> 0) { %>
                    <div class="grid md:grid-cols-3 gap-8">
                        <% news.forEach(item=> { %>
                            <article class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition">
                                <div class="h-48 bg-gray-200">
                                    <% if (item.image) { %>
                                        <img src="<%= item.image %>" alt="<%= item.title %>"
                                            class="w-full h-full object-cover">
                                        <% } else { %>
                                            <div class="w-full h-full flex items-center justify-center">
                                                <i class="fas fa-newspaper text-4xl text-gray-400"></i>
                                            </div>
                                            <% } %>
                                </div>
                                <div class="p-6">
                                    <h3 class="font-bold text-lg mb-2 text-gray-800">
                                        <a href="/berita/<%= item.slug %>" class="hover:text-orange-600">
                                            <%= item.title %>
                                        </a>
                                    </h3>
                                    <p class="text-gray-600 text-sm mb-4">
                                        <%= item.content.substring(0, 100) %>...
                                    </p>
                                    <div class="flex justify-between items-center text-sm text-gray-500">
                                        <span><i class="fas fa-calendar mr-1"></i>
                                            <%= new Date(item.created_at).toLocaleDateString('id-ID') %>
                                        </span>
                                        <span><i class="fas fa-eye mr-1"></i>
                                            <%= item.views %>
                                        </span>
                                    </div>
                                </div>
                            </article>
                            <% }) %>
                    </div>
                    <% } else { %>
                        <p class="text-center text-gray-500">Belum ada berita.</p>
                        <% } %>
            </div>
        </section>

        <!-- CTA Section -->
        <section class="bg-gradient-to-b from-orange-400 to-orange-700 text-white py-16">
            <div class="max-w-4xl mx-auto px-4 text-center">
                <h2 class="text-3xl font-bold mb-4">Bergabung Bersama Kami</h2>
                <p class="text-lg mb-8 opacity-90">
                    Daftarkan diri Anda sekarang dan jadilah bagian dari komunitas kami
                </p>
                <a href="/register"
                    class="bg-white text-orange-600 px-8 py-3 rounded-lg font-semibold hover:bg-gray-100">
                    Daftar Sekarang
                </a>
            </div>
        </section>